<%= form_for(@refable, html: {class: "form-horizontal"}) do |f| %>
  <%= f.hidden_field(:publisher_id) %>
  <% if @refable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@refable.errors.count, "error") %> prohibited this journal from being saved:</h2>

      <ul>
      <% @refable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, "Canonical Name", class: "control-label" %>
    <div class="controls">
      <%= f.text_field :name, { size: nil, class: "span12" } %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>

<% end %>
